
#include "user.h"
//int id = 0;
//int nr_teachers = 0;
int main()
{

	int choice=4;
	//USER* user = new USER[2]{ USER("guest","guest",0,0), USER("admin","admin",1,1) };
	//TEACHER* teacher = new TEACHER[1];
	//USER::nr_users = 2;
	// guest user and admin
	while (choice)
	{
		//initially user is set to 0 meaning guest user
		user[0].solve(choice);

	}
	delete[] teacher;
	delete[] user;
}